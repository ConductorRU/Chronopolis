#include "stdafx.h"
#include "Mesh.h"
#include "Planet.h"
namespace DEN
{
	Planet::Planet(InputLayout *layout): Mesh(layout)
	{
	
	}
	Planet::~Planet()
	{
	
	}
	void Planet::Generate(uint size, float radius)
	{
		for(int x = 0; x < size; ++x)
		{
			for(int y = 0; y < size; ++y)
			{
			
			}
		}
	}
};